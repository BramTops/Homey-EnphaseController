{
  "name": "nl.creitive.enlighten",
  "version": "1.3.5",
  "main": "app.js",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "lint": "eslint --ext .js,.ts --ignore-path .gitignore .",
    "version": "node -e \"const fs = require('fs'); const pkg = require('./package.json'); const cmp = require('./.homeycompose/app.json'); cmp.version = pkg.version; fs.writeFileSync('./.homeycompose/app.json', JSON.stringify(cmp, null, 2));\""
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/homey": "npm:homey-apps-sdk-v3-types@^0.3.12",
    "eslint": "^7.32.0",
    "eslint-config-athom": "^3.1.5"
  }
}
